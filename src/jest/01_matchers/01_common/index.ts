export const sum = (a: number, b: number): number => {
    return a + b;
};

export interface Person {
    name: string;
    profession: string;
}

export const data: Person = {
    name: "John Torturro",
    profession: "Actor",
};
